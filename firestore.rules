rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // 1. Users collection
    match /users/{userId} {
      // Telegram mini app users can read, create, and update their own docs without Firebase Auth
      allow read, write: if true;
    }
    match /users/{document=**} {
      allow read, write: if true;
    }

    // 2. Admin settings (Global configuration)
    match /adminSettings/{document=**} {
      // Anyone can read settings, only authenticated admins can write
      allow read: if true;
      allow write: if request.auth != null;
    }

    // 3. Tasks configuration
    match /tasks/{document=**} {
      // Anyone can read tasks, only authenticated admins can write
      allow read: if true;
      allow write: if request.auth != null;
    }

    // 4. User task completions
    match /userTasks/{document=**} {
      allow read, write: if true;
    }

    // 5. Referrals
    match /referrals/{document=**} {
      allow read, write: if true;
    }

    // 6. Withdraw requests
    match /withdrawRequests/{document=**} {
      allow read, write: if true;
    }

    // 7. Audit logs
    match /auditLogs/{document=**} {
      allow read, write: if true;
    }

    // 8. Security events
    match /securityEvents/{document=**} {
      allow read, write: if true;
    }
  }
}
